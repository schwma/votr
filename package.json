{
  "name": "votr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_ENV=test mocha --recursive",
    "format:test": "prettier --list-different \"{src,test,config}/**/*.{js,json,css,md}\"",
    "format": "prettier --write \"{src,test,config}/**/*.{js,json,css,md}\""
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "{src,test,config}/**/*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "Marcel Schwarz",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.3",
    "chai": "^4.2.0",
    "crypto-extra": "^0.4.0",
    "express": "^4.16.4",
    "faker": "^4.1.0",
    "sequelize": "^4.42.0",
    "sqlite3": "^4.0.4"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.4",
    "mocha": "^5.2.0",
    "prettier": "1.16.4",
    "supertest": "^3.4.2"
  }
}
