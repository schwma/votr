{
  "name": "votr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_ENV=test mocha --recursive",
    "format:test": "prettier --list-different \"{src,test,config,docs}/**/*.{js,json,css,md}\"",
    "format": "prettier --write \"{src,test,config,docs}/**/*.{js,json,css,md}\"",
    "coverage": "NODE_ENV=test nyc mocha --recursive",
    "coverage-html": "NODE_ENV=test nyc --reporter=html mocha --recursive"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "author": "Marcel Schwarz",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.5",
    "chai": "^4.2.0",
    "crypto-extra": "^0.4.0",
    "express": "^4.17.1",
    "faker": "^4.1.0",
    "sequelize": "^4.44.0",
    "sqlite3": "^4.0.8"
  },
  "devDependencies": {
    "husky": "^1.3.1",
    "lint-staged": "^8.1.7",
    "mocha": "^5.2.0",
    "nyc": "^14.1.1",
    "prettier": "1.16.4",
    "supertest": "^3.4.2"
  }
}
